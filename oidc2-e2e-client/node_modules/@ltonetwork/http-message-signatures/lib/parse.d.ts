import { Component, HeaderValue, Parameters } from './types';
export declare function parseSignatureInputHeader(header: HeaderValue): {
    key: string;
    components: Component[];
    parameters: Parameters;
};
export declare function parseAcceptSignatureHeader(header: HeaderValue): {
    key: string;
    components: Component[];
    parameters: Parameters;
};
export declare function parseSignatureHeader(key: any, header: HeaderValue): Uint8Array;
