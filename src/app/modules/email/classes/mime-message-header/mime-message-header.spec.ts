import { MimeMessageHeader } from "./mime-message-header";

describe('MimeMessageHeader', () => {
  it('should create an instance', () => {
    expect(new MimeMessageHeader()).toBeTruthy();
  });
});